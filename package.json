{
  "name": "leave-messages",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "web": "cd ./web && npm run dev",
    "start": "npm run mongo && npm run web & npm run server",
    "mongo":"mongod --config /usr/local/etc/mongod.conf",
    "server": "cd ./server && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^17.0.40",
    "@types/react-dom": "^17.0.13",
    "vite": "^2.8.6"
  },
  "dependencies": {
    "redis": "^4.0.4"
  }
}
